apiVersion: v1
kind: Service
metadata:
  name: job-service
  namespace: nimbusflow

  labels:
    app: job-service

spec:
  selector:
    app: job-service

  ports:
    - name: http
      port: 8082
      targetPort: 8082