apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: discovery-server-monitor
  namespace: monitoring

  labels:
    release: monitoring

spec:
  selector:
    matchLabels:
      app: discovery-server

  namespaceSelector:
    matchNames:
      - nimbusflow

  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s