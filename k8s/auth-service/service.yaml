apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: nimbusflow

  labels:
    app: auth-service

spec:
  selector:
    app: auth-service

  ports:
    - name: http
      port: 8084
      targetPort: 8084