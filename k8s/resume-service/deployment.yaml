apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume-service
  namespace: nimbusflow

spec:
  replicas: 1

  selector:
    matchLabels:
      app: resume-service

  template:
    metadata:
      labels:
        app: resume-service

    spec:
      containers:

        - name: resume-service

          image: sapan18/resume-service:v2

          ports:
            - containerPort: 8081

          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: nimbusflow-config
                  key: POSTGRES_DB

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: nimbusflow-secret
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nimbusflow-secret
                  key: POSTGRES_PASSWORD

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081

            initialDelaySeconds: 60
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081

            initialDelaySeconds: 30
            periodSeconds: 10